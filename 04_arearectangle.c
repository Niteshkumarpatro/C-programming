#include<stdio.h>
#include<conio.h>
 int main()
 {
    int length,width,area;
    length=2;
    width=8;
    area=(length*width);
    printf("%d",area);
 }
//  area of rectangle = length*width(breadth)
// perimeter =2*(length+width)